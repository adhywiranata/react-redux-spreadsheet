const initialState = {
  cellCursor: 'A1',
  colCursor: '',
  isEditing: false,
};

const gridCursorReducer = (state = initialState, action) => {
  return state;
};

export default gridCursorReducer;
